{
  "name": "monorepo",
  "private": true,
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "server": "cd server && node index.js",
    "client": "cd client && npm start"
  },
  "dependencies": {
    "dotenv": "^16.1.4"
  }
}
